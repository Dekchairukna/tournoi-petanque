# รายการจัดระเบียบไฟล์

ไฟล์นี้สร้างจากการจัดระเบียบโปรเจกต์: ลบไฟล์ซ้ำ ไฟล์ทดลอง ไฟล์ชื่อเพี้ยน และไฟล์อัปโหลดตัวอย่างที่ไม่ได้ใช้ในระบบหลัก

## ไฟล์ที่ลบออก

- `models 2.py` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `TBF tournament 2.xlsx` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `swiss_logic 2.py` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `requirements 2.txt` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `standings 2.py` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `swiss_system 2.py` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/manual_pairings 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/playoff_match_tables 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/admin_users 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/403 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/admin_edit_user 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/next_round_robin_setup 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/playoff_online_scorecard 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/round_matches 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/playoff_print_report 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/standings 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/1 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/score_sheet 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/event_standings 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/event 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/create_event 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/next_knockout_setup 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/index 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/login 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/playoff_score_sheet 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/match_pairs 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/base 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/bracket_setup 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/admin_manual_pairings 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/admin_add_user 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/playoff_detail 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/macros 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/random_pairs 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/next_double_knockout_setup 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/online_scorecard 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `templates/edit_team 2.html` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `Procfile 2` — ไฟล์สำเนาซ้ำชื่อมี ' 2' และมีไฟล์ชื่อหลักใช้งานอยู่แล้ว
- `α╕úα╕░α╕Üα╕Ü.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `α╕äα╕╣α╣êα╕íα╕╖α╕¡.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `╬▒Γòòa╠ê╬▒ΓòòΓòú╬▒Γòúe╠é╬▒Γòòi╠ü╬▒ΓòòΓòû╬▒Γòò┬í.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `Γò¼ΓûÆ╬ôo╠Ço╠ÇΓö£ΓòæΓò¼ΓûÆ╬ôo╠Ço╠Ç╬ôu╠é├ªΓò¼ΓûÆ╬ôo╠Ço╠ÇΓö£┬úΓò¼ΓûÆ╬ôo╠Ço╠ÇΓö£┬ú.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `╬▒Γòòu╠ü╬▒ΓòòΓûæ╬▒ΓòòU╠ê╬▒ΓòòU╠ê.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `α╕ùα╕│α╣âα╕½α╣ëα╣éα╕¢α╕úα╣Çα╕êα╕üα╕òα╣îα╕¬α╕▓α╕íα╕▓α╕úα╕û.docx` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `#U0e04#U0e39#U0e48#U0e21#U0e37#U0e2d.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `#U0e23#U0e30#U0e1a#U0e1a.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `Γò¼ΓûÆ╬ôo╠Ço╠ÇuΓòáu╠êΓò¼ΓûÆ╬ôo╠Ço╠Ç╬ôu╠é├ªΓò¼ΓûÆ╬ôo╠Ço╠ÇUΓòáe╠éΓò¼ΓûÆ╬ôo╠Ço╠ÇUΓòáe╠é.txt` — ไฟล์ชื่อเพี้ยน/ไฟล์เอกสารชั่วคราว ไม่ได้ถูก import ใช้งานในระบบ
- `main` — ไฟล์ว่าง/ไฟล์ทดลอง ไม่ได้ถูก import ใช้งานในระบบ
- `1.py` — ไฟล์ว่าง/ไฟล์ทดลอง ไม่ได้ถูก import ใช้งานในระบบ
- `uploads/Team.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads/Book1.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads/test.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads/test_event.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads/test_output.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads/123.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads/A.xlsx` — ไฟล์ upload ตัวอย่าง/ทดสอบ ไม่ใช่ไฟล์โค้ดของระบบ
- `uploads` — โฟลเดอร์ว่างหลังลบไฟล์ทดสอบ

## ไฟล์หลักที่คงไว้

- `app.py`
- `models.py`
- `standings.py`
- `swiss_logic.py`
- `swiss_system.py`
- `templates/`
- `static/`
- `instance/tournoi.db`
- `requirements.txt`
- `Procfile`
